#include <stdio.h>

int main() {
  float fDeg, cDeg;
  scanf("%f", &fDeg);
  cDeg = (fDeg-32) * 5.0/9;
  printf("centigrade = %f\n", cDeg);
  printf("percentage = %f\n", ttlMks*100/500);
  return 0;
}

